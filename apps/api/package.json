{
  "name": "@caneca-facil/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "pretest": "npm run build --workspace @caneca-facil/core",
    "test": "vitest run",
    "pretypecheck": "npm run build --workspace @caneca-facil/core",
    "typecheck": "tsc -p tsconfig.json",
    "prebuild": "npm run build --workspace @caneca-facil/core",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@caneca-facil/core": "0.1.0",
    "@hono/node-server": "2.1.1",
    "@supabase/supabase-js": "2.116.0",
    "hono": "4.13.7",
    "openai": "7.15.0"
  },
  "devDependencies": {
    "@types/node": "24.13.4"
  }
}
